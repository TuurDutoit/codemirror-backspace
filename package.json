{
  "name": "codemirror-backspace",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "parcel index.html",
    "clean": "rm -rf .cache dist",
    "start:clean": "yarn start && yarn clean"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@codemirror/basic-setup": "^0.17.1",
    "@codemirror/lang-python": "^0.17.2",
    "@codemirror/lang-sql": "^0.17.1"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.4",
    "typescript": "^4.1.3"
  }
}
